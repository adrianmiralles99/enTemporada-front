import { FavoritosEntrada } from './favoritos-entrada.model';

describe('FavoritosEntrada', () => {
  it('should create an instance', () => {
    expect(new FavoritosEntrada()).toBeTruthy();
  });
});
