import { LikesSubcomentario } from './likes-subcomentario.model';

describe('LikesSubcomentario', () => {
  it('should create an instance', () => {
    expect(new LikesSubcomentario()).toBeTruthy();
  });
});
